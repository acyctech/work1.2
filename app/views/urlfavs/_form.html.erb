<%= form_for(urlfav) do |f| %>
  <% if urlfav.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(urlfav.errors.count, "error") %> prohibited this urlfav from being saved:</h2>

      <ul>
      <% urlfav.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %>
    <%= f.text_area :url %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
